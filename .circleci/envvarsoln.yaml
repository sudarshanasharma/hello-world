version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          name: "echo an env var that is part of our project"
          command: |
            echo $MY_NAME
 workflows:
  test-env-vars:
    jobs:
      - build
